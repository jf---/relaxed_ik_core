#import "../Temple/lib.typ": *
#import "@preview/gentle-clues:0.9.0": *
#import "@preview/glossarium:0.4.1": make-glossary, print-glossary, gls, glspl

// #show make-glossary

#let mit = toml("../mit.toml")

== Aanleiding <aanleiding>

In de ontwikkeling van #mit.AM technologie vind een belangrijke verschuiving
plaats. Waar in de 2010's de focus op het ontwikkelen van _proces_ aspecten de
ontwikkeling van #mit.AM hebben gedomineerd, (_hoe_ je metaal print, _hoe_ je
beton extrudeert, _hoe_ aarde wordt gespoten) lang hebben gedomineerd, is er
sprake van *technologische convergentie* op het vlak van proces. Daarmee is _hoe_ beton
wordt geëxtrudeerd, hoe metaal in een #mit.WAAM proces wordt verwerkt, minder
onderscheidend.

#task[
  - #mit.se3dp net als #mit.sc3dp kent eigenschappen waardoor de wijze waarop toolpaden tot stand komen, nog relatief nieuw is.
]

Meer de interactie tussen proces en materiaal die hierin gekend wordt.

Nu deze proces interacties in belangrijke mate gestabiliseerd en onderkend zijn
op proces en materiaal niveau, wordt #mit.AM onderscheidend op een volgende
niveau, namelijk de interactie tussen proces en geometrie.

Hoe toolpaden worden gegenereerd, kent een grote marge, een die die proces
parameters zelfs overstijgt.

De wijze *hoe* een #mit.AM product is opgebouwd -- hoe de toolpaden zijn
opgebouwd is *tenminste zo onderscheidend* als de wijze waarop het print proces
beheerst wordt. (de impact van het proces)

== Je innovatie

== Meerwaarde

== Markt


@waam, en als je er nog eens aan stipt, @waam, dan moet je het zeker ook eens over @se, en @se3dp hebben zeker, @se3dp

@uliege



